services:
  - type: web
    name: blood-donation-system
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate --noinput
    startCommand: gunicorn blood_donation_system.wsgi:application
